# 公開画面の確認記録

確認日: 2026-08-29

## 料金表ページ (`/`)

- 料金表の全コンテンツが表示されることを確認した。
- 追加した「初回体験・ご予約はこちら」および「メニューを相談して予約する」の予約導線が表示されることを確認した。
- 料金表の各セクションへ移動するアンカーリンクが表示されることを確認した。

## 予約フォーム (`/reservation`)

- 料金表の「初回体験・ご予約はこちら」から `/reservation` へ遷移することを確認した。
- 週表示の日付選択、LINE問い合わせ導線、要望入力、お客様情報入力、個人情報同意、送信ボタンが表示されることを確認した。
- 実データベースおよび通知用環境変数はローカル環境に設定されていないため、予約送信の実行は行わなかった。

## 制約

- ローカル起動時は OAuth およびデータベース関連の環境変数が未設定であるため、スタッフ管理画面の認証後操作は実行できない。
- OAuth設定が未投入のローカル環境で `/reservations` を開いた場合、画面がクラッシュせず公開ページへ戻ることを確認した。
- `pnpm check` と `pnpm build` が成功することを確認した。ビルド時に発生していた未設定の分析環境変数に関する警告は、設定時のみ計測スクリプトを読み込む方式に変更して解消した。
- 本番デプロイ前には DATABASE_URL、OAUTH_SERVER_URL、VITE_OAUTH_PORTAL_URL、VITE_APP_ID、LINE_CHANNEL_ACCESS_TOKEN、RESEND_API_KEY など、必要な環境変数を設定する必要がある。
