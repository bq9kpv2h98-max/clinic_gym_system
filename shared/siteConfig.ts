/**
 * ===================================================
 * siteConfig.ts — 店舗固有情報の一元管理ファイル
 * ===================================================
 * 新しい店舗にシステムを展開する際は、このファイルだけを
 * 書き換えてください。他のファイルを変更する必要はありません。
 *
 * 変更が必要な項目:
 *   1. 店舗名・ブランド名
 *   2. 連絡先（LINE・電話番号）
 *   3. 所在地（Googleマップリンク）
 *   4. 業種・キャッチコピー
 *   5. 予約フォームのメニュー
 *   6. 営業時間・定休日
 */

export const siteConfig = {
  // ─────────────────────────────────────────────
  // 1. 店舗名・ブランド
  // ─────────────────────────────────────────────
  /** 管理画面・メールの署名などに使う正式名称 */
  name: "ULU整骨院 ULU GYM",
  /** 予約フォームのヘッダー・フッターに表示するブランド名 */
  brandName: "ULU GROUP",
  /** 決算報告書・コピーライトに使う短縮名 */
  shortName: "ULU Group",
  /** 業種説明（ランディングページのキャッチコピー用） */
  businessType: "整体院・パーソナルジム",

  // ─────────────────────────────────────────────
  // 2. 連絡先
  // ─────────────────────────────────────────────
  /** 予約フォームの問い合わせ先LINE（顧客向け） */
  lineUrlPublic: "https://lin.ee/pvsHToe",
  /** メール・確定通知のお問い合わせ先LINE */
  lineUrlContact: "https://lin.ee/u7Y0twE",
  /** 電話番号（任意・未使用の場合は空文字） */
  phoneNumber: "",

  // ─────────────────────────────────────────────
  // 3. 所在地
  // ─────────────────────────────────────────────
  /** Googleマップの短縮URL */
  googleMapsUrl: "https://maps.app.goo.gl/7Zh9LzwsMtpTFA6X8",
  /** 住所テキスト（任意） */
  address: "",

  // ─────────────────────────────────────────────
  // 4. システム設定
  // ─────────────────────────────────────────────
  /** 本番サイトのURL（ヘルスチェック・アンケートリンクに使用） */
  siteUrl: "https://ulu-connect.com",
  /** 施設ID（QRコード・予約管理の内部識別子） */
  facilityId: "facility-001",

  // ─────────────────────────────────────────────
  // 5. 予約フォーム設定
  // ─────────────────────────────────────────────
  /** 予約フォームのメニュー一覧 */
  reservationMenus: [
    { value: "initial", label: "初回体験（90分）" },
    { value: "regular", label: "通常施術（60分）" },
    { value: "gym", label: "パーソナルトレーニング（60分）" },
    { value: "combo", label: "整骨院＋ジムセット（90分）" },
  ],

  // ─────────────────────────────────────────────
  // 6. 営業時間・定休日
  // ─────────────────────────────────────────────
  /** 定休日の曜日（0=日, 1=月, ..., 6=土） */
  closedDays: [0], // 日曜定休
  /** 営業開始時刻（HH:MM形式） */
  openTime: "10:00",
  /** 営業終了時刻（HH:MM形式） */
  closeTime: "21:00",
  /** 予約スロットの間隔（分） */
  slotIntervalMinutes: 30,
  /** 施術時間（分）— カレンダーの終了時刻計算に使用 */
  appointmentDurationMinutes: 90,
} as const;

export type SiteConfig = typeof siteConfig;
